{
  "name": "node",
  "version": "1.0.0",
  "description": "DNS解析，建立TCP连接，发送http请求\r server接收到http请求，处理，并返回客户端接收到返回数据，\r 客户端处理数据（如渲染页面，执行js）",
  "main": "bin/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=dev nodemon ./bin/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.14"
  }
}